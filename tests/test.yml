---

- hosts: all
  become: 'yes'
  roles:
    - role: plumelo.letsencrypt
      letsencrypt_before:
        - 'echo "before1" >> /root/before1.test'
        - 'echo "before2" >> /root/before2.test'
      letsencrypt_after:
        - 'echo "after1" >> /root/after1.test'
        - 'echo "after2" >> /root/after2.test'
